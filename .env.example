# Google Cloud Configuration
# Copy this file to .env and fill in your actual values

# Choose Model Backend: 0 -> ML Dev, 1 -> Vertex AI
GOOGLE_GENAI_USE_VERTEXAI=1

# Vertex AI Configuration (required if GOOGLE_GENAI_USE_VERTEXAI=1)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Gemini Model
MODEL=gemini-2.0-flash

# Document AI Configuration (required for OCR)
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us
DOCUMENT_AI_PROCESSOR_ID=your-processor-id

# Google Cloud Credentials
# Set the path to your service account JSON key file
GOOGLE_APPLICATION_CREDENTIALS=./credentials/your-service-account-key.json

# Optional: API Rate Limiting (for benchmarking)
# MIN_REQUEST_INTERVAL=2.0  # seconds between API calls
# Initial project setup
